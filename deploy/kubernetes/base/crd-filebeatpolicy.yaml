apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: filebeatpolicies.filebeat.ops.io
spec:
  group: filebeat.ops.io
  scope: Namespaced
  names:
    plural: filebeatpolicies
    singular: filebeatpolicy
    kind: FilebeatPolicy
    shortNames: ["fbp"]
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                id: {type: string}
                name: {type: string}
                clusterID: {type: string}
                namespace: {type: string}
                namespaceSelector:
                  type: object
                  properties:
                    matchNames:
                      type: array
                      items: {type: string}
                controllerType: {type: string}
                controllerName: {type: string}
                containerName: {type: string}
                podSelector:
                  type: object
                  properties:
                    matchLabels:
                      type: object
                      additionalProperties: {type: string}
                nodeSelector:
                  type: object
                  properties:
                    matchLabels:
                      type: object
                      additionalProperties: {type: string}
                logType: {type: string}
                logPath: {type: string}
                enabled: {type: boolean}
                priority: {type: integer}
                customFields:
                  type: object
                  additionalProperties: {type: string}
            status:
              type: object
              properties:
                phase: {type: string}
                message: {type: string}
                policyID: {type: string}
                revision: {type: integer}
                checksum: {type: string}
                observedGeneration: {type: integer}
                lastSyncTime: {type: string}
